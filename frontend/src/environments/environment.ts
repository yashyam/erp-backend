/**
 * Override this value for a different backend host (or use Angular file
 * replacement in a deployment-specific configuration).
 */
export const environment = {
  production: false,
  apiBaseUrl: 'http://localhost:8080/api/v1',
  googleClientId: ''
};
